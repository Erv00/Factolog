// This module adds 3 to the input
async module add3(in i, out o){
    o = i+3;
}

//This module tests all of the operators
async module main(in i, out o){
    var a;
    add3(i, a);
    o = a+i-1 | a & 3 >> 1 ** i << 4 ^ (a * 3 / i) % 3;
}
